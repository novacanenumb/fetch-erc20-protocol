# Fetch ERC20 Protocol Production Handoff

```yaml
handoff:
  objective: "Maintain fetch-erc20-protocol in production under ORG_ORPHEUS."
  current_status: "Production root now contains the external build prompt, collapsible organisation task tree append, static embedded-browser orchestration demo, and admin-dashboard nested-org projection for Fetch ERC20 Protocol."
  files_touched:
    - "production/root/fetch-erc20-spec.md"
    - "production/root/index.md"
    - "production/root/docs/HANDOFF.md"
    - "production/root/app/fetch-erc20-demo/index.html"
    - "production/root/app/fetch-erc20-demo/README.md"
    - "05_APPLICATIONS/01_OPERATOR/03_DASHBOARDS/nested_org_control_plane/apps/web/src/features/dashboard-pages/DashboardRouter.tsx"
    - "05_APPLICATIONS/01_OPERATOR/03_DASHBOARDS/nested_org_control_plane/packages/protocol-types/mock_protocol_snapshot.json"
  decisions_made:
    - "This root is isolated and must bootstrap through the Orpheus root protocol."
    - "production/root is the current planning, review, and orchestration surface."
    - "Protocol implementation remains sandbox-root gated unless nova or Fetch grants a separate written implementation or promotion lease."
    - "Three GPT-5.4-mini agent lanes are active: Tesla for compliance, Raman for architecture decomposition, and Hilbert for QA/test planning."
    - "Tesla's compliance caveats were integrated into the task tree authority notes."
    - "Raman's dependency chain and ten-packet implementation cut were integrated into the task tree."
    - "Hilbert's feature-gate test matrix and boundary cases were integrated into the Foundry validation job."
    - "Six live GPT-5.4-mini workers are assigned: Tesla medium, Raman high, Hilbert xhigh, Noether medium, Turing high, and Curie xhigh."
    - "Current worker assignment ids are Tesla=019f137c-fb39-7443-a60c-2229581b5f61, Raman=019f137c-fce3-7741-bf7f-091a63e57ea7, Hilbert=019f137c-fe37-78f3-9ca7-752fba4b9b9e, Noether=019f137c-ffb7-7563-9cb6-41dc026e14f3, Turing=019f137d-0137-7862-b35a-d6f9061630f7, Curie=019f137d-0291-77c1-9896-24c4d6013d51."
    - "Three Claude API lanes are represented as secure-gated provider agents without secret exposure or live credential assumptions."
    - "Claude readiness check found no ANTHROPIC_API_KEY, no CLAUDE_API_KEY, and no ORPHEUS_CLAUDE_MANAGED_AGENTS_ENABLED=true; Claude API lanes remain secure-gated."
    - "Local demo server was started at http://127.0.0.1:8787/ for live viewing."
    - "Meitner's protocol trace was integrated as the demo's full v0 trace preset."
    - "Euclid's Claude API safety guidance was integrated by keeping Claude lane UI non-secret-bearing and secure-gated."
    - "Fetch ERC20 Protocol is projected in the external main admin dashboard as a child organisation under ORG_ORPHEUS with its own scoped task tree."
    - "The production root does not own the admin dashboard artifact; dashboard evidence is recorded as an external control-plane projection."
    - "Raman audit found the static demo needed execution-grade packet dependency gates; the demo now includes an Implementation Packet Gates panel covering owners, prerequisites, write sets, and Packet 7/8/10 scope corrections."
    - "Tesla audit confirmed containment is mostly consistent and identified that admin-dashboard evidence must remain qualified as external to this root."
    - "Turing audit confirmed the default/all-org admin dashboard projection is adequate for showing Fetch ERC20 under ORG_ORPHEUS with its scoped task tree."
    - "Noether audit confirmed six GPT lanes, three Claude secure-gated lanes, task tree, full v0 trace, and secure-gated behavior are represented; readability gaps were fixed with expand/collapse controls, trace count feedback, taller trace pane, and stronger Claude locked-state copy."
    - "Hilbert audit confirmed Forge validation cannot currently be proven from this production root because no foundry.toml, Solidity sources, or .t.sol files are present here."
    - "Hilbert's additional missing test obligations were added to docs/FOUNDRY_TEST_PLAN.md: zero-supply redeem, non-creator rebalance, arbitrary external-call rejection, factory rejection of yield/LP/perp adapters, and rebalance rejection of yield/LP/perp adapters."
    - "The live demo was changed from task-interface-first to protocol-first by adding a Fetch ERC20 thesis-vault protocol console above the agent/job interface."
    - "The live demo now defaults to the Protocol Demo navigation item and uses a vibrant dark protocol theme with neon cyan, magenta, and green state cues."
    - "The protocol console now includes a live-readiness strip showing embedded-browser status, protocol simulation scope, six GPT lane representation, and Claude API secure-gated status."
    - "The protocol console now includes a one-click Run full protocol demo action that deterministically displays launch, buy/deposit, rebalance, and sell/redeem completion in the browser-local simulation."
    - "The embedded protocol console now auto-runs the full launch, buy/deposit, rebalance, and sell/redeem simulation shortly after page load, while preserving manual controls for replay, pause modes, stale oracle, and reset."
    - "The embedded protocol console now includes a live operations feed that cycles through all six GPT lanes and all three Claude API lanes, with Claude lanes explicitly shown as secure-gated rather than live provider executions."
    - "The embedded protocol console now includes an objective completion matrix that marks the embedded browser app and six GPT lanes as represented, the three Claude API lanes as blocked, and protocol execution as browser-local simulation rather than deployed contract evidence."
    - "The embedded protocol console now includes a Claude activation checklist describing the required provider path, managed-lane enable flag, and presence-only no-secret verification rule needed to promote Claude-A/B/C from secure-gated cards to live API workers."
    - "The embedded protocol console now includes an operator handoff block with the active objective, live demo URL, protocol simulation status, GPT lane representation state, Claude blocker, unblock requirements, safety rule, and next action."
    - "The embedded protocol console now includes a browser-local runtime heartbeat showing live timestamp, session elapsed time, auto-demo completion state, and Claude provider gate state."
    - "The embedded protocol console now includes a machine-readable evidence snapshot that exposes the live demo URL, dark theme, auto-run sequence, six GPT lane representation, three Claude secure-gated lanes, protocol simulation state, safety rule, and next action."
    - "The embedded protocol console now includes a protocol lifecycle map showing the simulated v0 SPOT flow: factory validation, vault launch, buy/deposit, creator rebalance, and sell/redeem."
  tests_run: []
  validation:
    - "Invoke-WebRequest http://127.0.0.1:8787/ returned HTTP 200 and confirmed expected title, GPT agents, Claude lanes, state inspector, and search controls."
    - "node --check passed for the embedded JavaScript extracted from app/fetch-erc20-demo/index.html."
    - "Claude readiness was checked by presence only; no secret values were printed."
    - "External admin dashboard dev server at http://127.0.0.1:8791/ returned HTTP 200."
    - "External rendered Edge/Playwright validation confirmed Fetch ERC20 Nested Organisation, Fetch ERC20 Task Tree, Fetch ERC20 Protocol, ORG_ORPHEUS, Packet jobs, and Blocked metrics are visible."
    - "Live demo at http://127.0.0.1:8787/ returned DEMO_HTTP_OK_PACKET_GATES after adding the packet-gates projection."
    - "Embedded demo JavaScript syntax validation passed after the packet-gates update."
    - "Rendered Edge validation confirmed task tree details count=11, expand-all opens all 11, collapse-all closes all, full trace shows 10 steps loaded, and Claude lanes show locked/no-approved-secret cues."
    - "Claude availability probe found ANTHROPIC_API_KEY, CLAUDE_API_KEY, ORPHEUS_CLAUDE_MANAGED_AGENTS_ENABLED, CLAUDE_AGENT_ENDPOINT, and ANTHROPIC_BASE_URL absent; only variable presence was checked, no secret values were printed."
    - "Rendered Edge validation confirmed the protocol console executes launch, buy/deposit, sell/redeem, rebalance, and stale-oracle actions; resulting state showed vault=HYP-vault-local, NAV=7,512.5, shares=7,500, PPS=1.0017, oracle=stale, and 50/50 WETH/WBTC composition after rebalance."
    - "Post-theme validation confirmed demo JavaScript syntax is valid, the protocol console appears before the task overview, the Protocol Demo nav item is current, and http://127.0.0.1:8787/ returns DEMO_HTTP_OK_DARK_PROTOCOL."
    - "Latest lightweight continuation audit read the referenced pasted prompt, confirmed Job 9 and Job 10 content markers, confirmed http://127.0.0.1:8787/ still returns DEMO_HTTP_OK_DARK_PROTOCOL, and confirmed ANTHROPIC_API_KEY, CLAUDE_API_KEY, ORPHEUS_CLAUDE_MANAGED_AGENTS_ENABLED, CLAUDE_AGENT_ENDPOINT, and ANTHROPIC_BASE_URL are absent by presence-only check."
    - "Latest live-readiness audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_LIVE_READINESS before the one-click full protocol demo control was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_FULL_DEMO_CONTROL before the page-load auto-run behavior was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_AUTO_FULL_DEMO before the live operations feed was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_LIVE_OPS before the objective completion matrix was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_COMPLETION_MATRIX before the Claude activation checklist was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_CLAUDE_ACTIVATION before the operator handoff block was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_OPERATOR_HANDOFF before the runtime heartbeat was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_RUNTIME_HEARTBEAT before the machine-readable evidence snapshot was added."
    - "Latest continuation audit confirmed http://127.0.0.1:8787/ returns DEMO_HTTP_OK_EVIDENCE_SNAPSHOT before the protocol lifecycle map was added."
  known_issues: []
  unresolved_risks:
    - "Environment-specific runtime has not been validated unless a later handoff says otherwise."
    - "The spec contains a deliberate authority split: current orchestration is in production/root, but implementation is still sandbox/root by default."
    - "No Claude API connector or approved Claude credential is exposed in this Codex environment, so the three Claude API agents remain secure-gated rather than live external workers."
    - "Claude secure-gating is currently a static/UI-level representation, not a live provider runtime enforcement path."
    - "Admin dashboard strict build currently fails on broader pre-existing TypeScript/module-resolution issues around @orpheus/protocol-types; runtime Vite display is verified."
    - "Dashboard org-scoped redaction may empty the Fetch task tree outside privileged/all-org views because redacted placeholders can lose org_id routing metadata."
    - "Curie completion-audit worker hit model-capacity/no-payload behavior and did not provide a usable completed audit."
    - "Forge validation is not executable from the current production root; the lease-approved sandbox package path is absent from this checkout."
  next_agent: "main orchestrator or delegated agent with verified path lease"
  next_required_action: "Collect the six GPT worker handoffs, then either provide an approved Claude API execution path or keep the Claude lanes secure-gated while starting Packet 1 only after confirming the intended implementation write root."
```
