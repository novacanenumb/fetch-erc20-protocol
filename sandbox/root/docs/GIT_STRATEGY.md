# Fetch ERC20 Protocol Sandbox Git Strategy

Do not initialise a nested `.git/` repository in this environment root during this migration. The current checkout is inside a parent Git repository, so this root uses `root/.gitkeep` and this strategy document as the placeholder strategy.

Keep `.agents/` local and untracked.
