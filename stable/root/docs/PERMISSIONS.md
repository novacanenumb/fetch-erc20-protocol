# Fetch ERC20 Protocol Stable Permission Rules

Default policy is deny by default. This root grants no implicit access to core engine files, sibling application roots, external tools, secrets, or provider credentials.

Access outside `06_ORGANISATIONS/ORG_ORPHEUS/fetch-erc20-protocol/stable/root/` requires an explicit permission file, contract, lease, or governance approval. Failure to verify access is a blocker.
